// Your code here

function createEmployeeRecord(firstName, familyName, title, payPerHour) {
 const employee = {
     firstName: `${firstName}`,
     familyName: `${familyName}`,
     title: `${title}`,
     payPerHour: `${payPerHour}`,
     timeInEvents: [],
     timeOutEvents: []
    
}
 return employee;
}

function createEmployeeRecords() {
    
}

// coming back to this later this makes no sense today
